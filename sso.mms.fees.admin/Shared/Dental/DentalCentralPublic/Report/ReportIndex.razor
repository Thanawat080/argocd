<div class="site-layout-background" style="padding: 24px;">
    <div class="mms-nav-title">
        <div class="mms-text-title px-4 py-3" style="color : black">ค่าบริการทางการแพทย์ทันตกรรม</div>
    </div>

    <div class="container py-4">
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
               
                <li class="breadcrumb-item" aria-current="page">
                    <span class="breadcum-process py-2">รายงาน </span>
                </li>

            </ol>
        </nav>
      
        <div class="row">
            <div class="d-flex  mms-media-center justify-content-end">

                <div class="col-4 ">
                    <Space Direction="@DirectionVHType.Vertical" Class="mms-btn-serch  col-12">
                        <SpaceItem>
                            <Search Placeholder="ชื่อสถานพยาบาล" EnterButton="true" />
                        </SpaceItem>
                    </Space>
                </div>
            </div>
        </div>
        <Table DataSource="listOfData" TItem="ItemData" Bordered ScrollX="1000">
            <ChildContent Context="data">
                <Column TData="int" Title="ลำดับ" Width="10%" Align="ColumnAlign.Center">
                   
                        @data.Id
                  
                   
                </Column>

                <Column TData="string" Title="รายงาน" Width="80%" Align="ColumnAlign.Left">
                   
                        @data.title
                   
                </Column>
                <ActionColumn Title="" Width="10%" Align="ColumnAlign.Center">
                   
                        
                        <img class="mx-2" src="helper_shared/PortalAdminImage/eye.png" style="width:24px;height:24px;"/>
                   

                </ActionColumn>
            </ChildContent>
        </Table>


    </div>
</div>

@if (isloading == true)
{
    <style>
        body {
            overflow: hidden;
        }</style>

    <LoadingPage></LoadingPage>
}

@using MatBlazor
@using System.ComponentModel.DataAnnotations;
@using System.Text.Json;
@using System.ComponentModel;
@inject ModalService _modalService
@code {
    [Parameter]
    public EventCallback<int> IndexPage { get; set; }


    public bool isloading = false;
    protected override void OnInitialized()
    {
        isloading = true;
        listOfData = new List<ItemData>
    {
     new ItemData
     {
         Id = 1,
         title = "รายงานข้อมูลกรณีทันตกรรมประจำปีเเยกรายเดือนเเยกตามสถานพยาบาล"
     },new ItemData
     {
         Id = 2,
         title = "รายงานคำสั่งจ่ายเเต่ละสถานพยาบาล"
     }

    };
        isloading = false;
        StateHasChanged();
    }
    record ItemData
    {
        public int Id { get; set; }
        public string title { get; set; }

    };

    IDictionary<string, (bool edit, ItemData data)> editCache = new Dictionary<string, (bool edit, ItemData data)>();
    List<ItemData> listOfData = new List<ItemData>();

    
   

    string title = "BasicModal";
    bool _visible = false;
    private async void HandleOk(MouseEventArgs e)
    {
        _visible = false;
    }

    private void HandleCancel(MouseEventArgs e)
    {
        Console.WriteLine(e);
        _visible = false;
    }
}

