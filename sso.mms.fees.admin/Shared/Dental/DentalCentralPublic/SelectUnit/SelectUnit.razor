

<div class="site-layout-background" style="padding: 24px;">
    <div class="mms-nav-title">
        <div class="mms-text-title px-4 py-3 "style="color :#000">ค่าบริการทางการแพทย์ทันตกรรม   </div>
    </div>
    <div class="container py-4">
        
        <nav aria-label="breadcrumb">
            <ol class="breadcrumb">
                <li class="breadcrumb-item" aria-current="page">
                    <span class="breadcum-success py-2">สืบค้นข้อมูล</span>
                </li>
            </ol>
        </nav>
        <div class="justify-content-center align-content-center">
            <div class="mms-media-center">
                <div class="col-6">
                    <div class="mms-media-center">
                        <div class="col-10">
                         <div style="font-size : 16px">เลือกหน่วยปฏิบัติงาน </div>
                        </div>
                    </div>
                    <div class="mms-media-center">
                        <div class="col-10">
                            <select value="@txtValue" class="form-select" @onchange="UpdateTxtValue" 
                                    aria-label="Default select example" style="text-align: center" >
                                <option>กรอกชื่อหน่วยปฏิบัติงานที่ต้องการสืบค้น</option>
                                <option value=1>สปส.เขต 1</option>
                                <option value=2>สปส.เขต 2</option>
                                <option value=3>สปส.เขต 3</option>
                                

                            </select>   
                        </div>
                    </div>
                </div>
            </div>
            <div class="mms-media-center my-3">
                
                    <img src="helper_shared/FeesExtImage/hopcheck.jpg" />
                
            </div>
        </div>

    </div>
</div>
<Button @onclick=@(() => OpenModal(true))></Button>

<AlertModal _visible="@ModalAlert" OnClose="@OpenModal"></AlertModal>

@using MatBlazor
@using System.ComponentModel.DataAnnotations;
@using System.Text.Json;
@using System.ComponentModel;
@using sso.mms.fees.admin.Shared.Dental.DentalCentralPublic.SearchProvince.Modals;
@code {
    [Parameter]
    public EventCallback<int> IndexPage { get; set; }
    int txtValue { get; set; }
    bool ModalAlert = false;
    string Placeholder = "วว/ดด/ปปปป";
    string style = "height:30px;line-height:30px;";

    bool loading = false;
    public async Task goPage(int item)
    {
        await IndexPage.InvokeAsync(item);
    }


    public void OpenModal(bool value)
    {
        ModalAlert = value;
    }

    private void UpdateTxtValue(ChangeEventArgs e)
    {
        txtValue = Convert.ToInt32(e.Value);

    }
    protected override void OnInitialized()
    {




    }

    List<ItemData> listOfData = new List<ItemData>
    {
        new ItemData
        {

        }
    };
    string _selectedValue;

    public class ItemData
    {
        public int Id { get; set; }
        public string Name { get; set; }
        public int Age { get; set; }
        public int count { get; set; }
        public string Address { get; set; }
    };




}
