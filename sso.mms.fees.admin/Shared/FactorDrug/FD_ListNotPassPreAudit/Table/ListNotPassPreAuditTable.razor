<Table DataSource="data" Bordered HidePagination >
    <Selection />
    <PropertyColumn Title="ลำดับ" Property="c=>c.No" Width="100px" />
    <PropertyColumn Title="เลขประจำตัวประชาชน" Property="c=>c.IDCard" Width="100px" />
    <PropertyColumn Title="ชื่อ-นามสกุล" Property="c=>c.Name" Width="100px" />
    <PropertyColumn Title="ประเภท" Property="c=>c.Type" Width="100px" />
    <PropertyColumn Title="วันที่รักษา" Property="c=>c.Treat" Width="100px" />
    <PropertyColumn Title="" Property="c=>c.AmountMoney" Align="ColumnAlign.Center" Width="100px" />
    <PropertyColumn Title="วันที่รักษา" Property="c=>c.Note" Width="100px" />
</Table>
<Pagination Class="py-3"
            ShowSizeChanger
            OnShowSizeChange="OnShowSizeChange"
            DefaultCurrent="3"
            Total="500" />

@code {

    record Data(
        string No,
        string IDCard,
        string Name,
        string Type,
        string Treat,
        string AmountMoney,
        string Note
    );

    Data[] data = new Data[]
    {
        new("1","1101201267892","นายดวงดี  วิชัยวงศ์","รุนแรงมาก","25/05/2566","29,600.00","ไม่ได้เป็นผู้ประกันตน"),
        new("2","1102345678925","นายนรง มีศักดิ์","รุนแรงปานกลาง","25/05/2566","29,600.00","ไม่ได้เป็นพนักงาน เกิน 6 เดือน"),
        new("3","1108954632579","นายยิ้มแย้ม แจ่มใส","รุนแรงน้อย","25/05/2566","29,600.00","ไม่ได้จ่ายเงินสมทบทุน 3 เดือน"),
    };

    void OnChange(QueryModel<Data> query)
    {
        Console.WriteLine(JsonSerializer.Serialize(query));
    }

    private void OnShowSizeChange(PaginationEventArgs args)
    {
        var (current, pageSize) = args;
        Console.WriteLine($"{current}, {pageSize}");
    }

    private void ViewDetail(int open)
    {
    }
}