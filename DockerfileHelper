FROM mcr.microsoft.com/dotnet/aspnet:6.0 AS base
WORKDIR /app
EXPOSE 80
EXPOSE 443

FROM mcr.microsoft.com/dotnet/sdk:6.0 AS build
WORKDIR /src
COPY ["./sso.mms.helper/sso.mms.helper.csproj", "sso.mms.helper/"]
RUN dotnet restore "sso.mms.helper/sso.mms.helper.csproj"
COPY . .
WORKDIR "/src/sso.mms.helper"
RUN dotnet build "./sso.mms.helper.csproj" -c Release -o /app/build

FROM build AS publish
RUN dotnet publish "./sso.mms.helper.csproj" -c Release -o /app/publish /p:UseAppHost=false

FROM base AS final
WORKDIR /app
COPY --from=publish /app/publish .
ENTRYPOINT ["dotnet", "sso.mms.helper.dll"]
